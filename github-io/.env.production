# Public keyId for the puzzle SDK key (origin allowlist:
# https://cadbuildr.github.io). Created via hub Settings → SDK partner keys.
VITE_CADBUILDR_SDK_KEY_ID=c60e49b36681e75d
# Override hub host because the key lives on the dev hub instance for now.
# To flip to prod (`https://hub.cadbuildr.com`), mint a fresh key on
# prod hub Settings and drop this line (SDK default is prod hub).
VITE_CADBUILDR_HUB_BASE_URL=https://hub-yg72zqen7a-uw.a.run.app
