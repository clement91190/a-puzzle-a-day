# Public keyId for the puzzle SDK key. The secret half NEVER goes here —
# the hub enforces the partner's allowed_origins allowlist against the
# browser's Origin header so this `keyId` cannot be reused from another site.
VITE_CADBUILDR_SDK_KEY_ID=624fe53d82c9fa7c
